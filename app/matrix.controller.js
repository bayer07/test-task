'use strict';

var angular = require('angular');

angular.module('matrix').controller('matrixController', MatrixController);

MatrixController.$inject = ['$scope'];

function MatrixController($scope) {
    var _this = this;
}
